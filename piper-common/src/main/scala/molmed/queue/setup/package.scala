package molmed.queue

/**
 * Classes relating to setting up a project. Contains code to create and read piper setup xml files, and
 * code relating to reading run reports to import potentially interegatk information.
 */
package object setup {
}